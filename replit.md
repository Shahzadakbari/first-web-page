# Running the project

This is a static web page. The Replit workflow serves the project directory with
Python's built-in HTTP server on port 5000:

```bash
python3 -m http.server 5000 --bind 0.0.0.0
```

Open the Replit preview to view `index.html`.